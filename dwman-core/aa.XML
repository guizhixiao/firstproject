<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dwman.biz.AdminMapper">
  <update id="update" parameterType="java.util.Map" >
    UPDATE SYS_ADMIN
      <include refid="UPDATE_SQL"/>
    WHERE id=#{id}
  </update>

    <insert id="insert" parameterType="java.util.Map">
        INSERT  INTO
        SYS_ADMIN
        (
          [for][params]###[name],[/for]
        )
        VALUES
        (
          [for][params]####{[name]},[/for]
        )
    </insert>
  <delete id="deleteById" parameterType="java.lang.Long">
    DELETE  FROM SYS_ADMIN
    WHERE ID=#{id}
  </delete>

  <select id="findById" parameterType="java.lang.Long" resultType="java.util.Map">
    SELECT * FROM SYS_ADMIN
    WHERE ID=#{id}
  </select>

  <select id="findAllByParam" parameterType="java.util.Map" resultType="java.util.Map">
    SELECT
    <include refid="COLUMN_SQL"/>
    FROM SYS_ADMIN
    WHERE 1 =1
    <include refid="WHERE_SQL"/>
  </select>

  <select id="findCount" parameterType="java.util.Map" resultType="java.lang.Long">
    select COUNT(1) from SYS_ADMIN
    WHERE  1=1
    <include refid="WHERE_SQL"/>
  </select>

  <select id="findAllByPageParam" parameterType="java.util.Map" resultType="java.util.Map">
    SELECT
    <include refid="COLUMN_SQL"/>
    FROM SYS_ADMIN
    WHERE 1=1
    <include refid="WHERE_SQL"/>
  </select>

  <select id="findCountByPageParam" parameterType="java.util.Map" resultType="java.lang.Long">
    SELECT COUNT(1) size
    FROM SYS_ADMIN
    WHERE 1=1
    <include refid="WHERE_SQL"/>
  </select>

  <sql id="COLUMN_SQL">
    [for][params]###[name] as [name],[/for]
  </sql>

  <sql id="WHERE_SQL">
      [for][params]###<if test="[name] != null">  AND [name] = #{[name]}  </if>[/for]
  </sql>

  <sql id="UPDATE_SQL">
      <set>
          [for][params]###<if test="[name]!=null">   [name]=#{[name]},  </if>[/for]
      </set>
  </sql>
</mapper>
